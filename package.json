{
  "name": "heroappseed",
  "version": "1.0.0",
  "description": "Seed for HeroAngularApp",
  "main": "seed.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./data,./hero.seed.json,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "npm run build && node dist/seed.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.1.2",
    "firebase-admin": "^5.0.0",
    "google-cloud": "^0.55.0",
    "lodash": "^4.17.4",
    "mime": "^1.3.6"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.1"
  }
}
